"use client";
import React, { useState } from "react";

const Page = async ({ params }) => {
  
  const { cat } = params;
  const getCategory = () => {};
  return (
    <div>
      <h1>{categories}</h1>
    </div>
  );
};

export default Page;
